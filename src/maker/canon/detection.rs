// AUTO-GENERATED by exiftool_sync extract maker-detection
// Source: third-party/exiftool/lib/Image/ExifTool/CANON.pm
// Generated: 2025-06-24
// DO NOT EDIT - Regenerate with `cargo run --bin exiftool_sync extract maker-detection`

#![doc = "EXIFTOOL-SOURCE: lib/Image/ExifTool/CANON.pm"]

/// Detection patterns for canon maker notes
#[derive(Debug, Clone, PartialEq)]
pub struct CANONDetectionResult {
    pub version: Option<u8>,
    pub ifd_offset: usize,
    pub description: String,
}

/// Detect canon maker note format and extract version information
/// 
/// Returns Some(DetectionResult) if this appears to be a canon maker note,
/// None otherwise.
pub fn detect_canon_maker_note(data: &[u8]) -> Option<CANONDetectionResult> {
    // Pattern from source line 0: Canon maker note
    if data.len() >= 5 && data.starts_with(&[0x43, 0x61, 0x6e, 0x6f, 0x6e]) {
        return Some(CANONDetectionResult {
            version: None,
            ifd_offset: 0,
            description: "Canon maker note".to_string(),
        });
    }

    // Pattern from source line 9400: canon maker note signature
    if data.len() >= 5 && data.starts_with(&[0x43, 0x61, 0x6e, 0x6f, 0x6e]) {
        return Some(CANONDetectionResult {
            version: None,
            ifd_offset: 0,
            description: "canon maker note signature".to_string(),
        });
    }

    None
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_canon_detection_pattern_0() {
        let test_data = &[0x43, 0x61, 0x6e, 0x6f, 0x6e];
        let result = detect_canon_maker_note(test_data);
        assert!(result.is_some());
        let detection = result.unwrap();
        assert_eq!(detection.version, None);
        assert_eq!(detection.ifd_offset, 0);
    }

    #[test]
    fn test_canon_detection_pattern_1() {
        let test_data = &[0x43, 0x61, 0x6e, 0x6f, 0x6e];
        let result = detect_canon_maker_note(test_data);
        assert!(result.is_some());
        let detection = result.unwrap();
        assert_eq!(detection.version, None);
        assert_eq!(detection.ifd_offset, 0);
    }

}
