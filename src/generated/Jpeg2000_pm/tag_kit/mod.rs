//! Modular tag kits with embedded PrintConv for Jpeg2000_pm
//!
//! This file is automatically generated by codegen.
//! DO NOT EDIT MANUALLY - changes will be overwritten.
//!
//! Generated from: Jpeg2000.pm table: CaptureResolution
//!
#![allow(unused_imports)]
#![allow(unused_mut)]
#![allow(dead_code)]
#![allow(unused_variables)]

pub mod interop;
pub mod other;

use crate::expressions::ExpressionEvaluator;
use crate::tiff_types::ByteOrder;
use crate::types::{Result, TagValue};
use std::collections::HashMap;
use std::sync::LazyLock;

#[derive(Debug, Clone)]
pub struct TagKitDef {
    pub id: u32,
    pub name: &'static str,
    pub format: &'static str,
    pub groups: HashMap<&'static str, &'static str>,
    pub writable: bool,
    pub notes: Option<&'static str>,
    pub print_conv: PrintConvType,
    pub value_conv: Option<&'static str>,
    pub subdirectory: Option<SubDirectoryType>,
}

#[derive(Debug, Clone)]
pub enum PrintConvType {
    None,
    Simple(&'static HashMap<String, &'static str>),
    Expression(&'static str),
    Manual(&'static str),
}

/// Type alias for subdirectory processor function
pub type SubDirectoryProcessor =
    fn(&[u8], ByteOrder, Option<&str>) -> Result<Vec<(String, TagValue)>>;

#[derive(Debug, Clone)]
pub enum SubDirectoryType {
    Binary { processor: SubDirectoryProcessor },
}

/// All tag kits for Jpeg2000_pm
pub static JPEG2000_PM_TAG_KITS: LazyLock<HashMap<u32, TagKitDef>> = LazyLock::new(|| {
    let mut map: HashMap<u32, TagKitDef> = HashMap::new();

    // interop tags
    for (id, tag_def) in interop::get_interop_tags() {
        // Priority insertion: preserve existing entries with subdirectory processors
        match map.get(&id) {
            Some(existing) if existing.subdirectory.is_some() => {
                // Keep existing tag if it has a subdirectory processor
                if tag_def.subdirectory.is_none() {
                    // Skip this tag - existing one is more important
                    continue;
                }
            }
            _ => {}
        }
        map.insert(id, tag_def);
    }

    // other tags
    for (id, tag_def) in other::get_other_tags() {
        // Priority insertion: preserve existing entries with subdirectory processors
        match map.get(&id) {
            Some(existing) if existing.subdirectory.is_some() => {
                // Keep existing tag if it has a subdirectory processor
                if tag_def.subdirectory.is_none() {
                    // Skip this tag - existing one is more important
                    continue;
                }
            }
            _ => {}
        }
        map.insert(id, tag_def);
    }

    map
});

// Helper functions for reading binary data
fn model_matches(model: &str, pattern: &str) -> bool {
    // ExifTool regexes are already in a compatible format
    // Just need to ensure proper escaping was preserved

    match regex::Regex::new(pattern) {
        Ok(re) => re.is_match(model),
        Err(e) => {
            tracing::warn!("Failed to compile model regex '{}': {}", pattern, e);
            false
        }
    }
}

fn format_matches(format: &str, pattern: &str) -> bool {
    if let Ok(re) = regex::Regex::new(pattern) {
        re.is_match(format)
    } else {
        tracing::warn!("Failed to compile format regex: {}", pattern);
        false
    }
}

fn read_int16s_array(data: &[u8], byte_order: ByteOrder, count: usize) -> Result<Vec<i16>> {
    if data.len() < count * 2 {
        return Err(crate::types::ExifError::ParseError(
            "Insufficient data for int16s array".to_string(),
        ));
    }
    let mut values = Vec::with_capacity(count);
    for i in 0..count {
        let offset = i * 2;
        let value = match byte_order {
            ByteOrder::LittleEndian => i16::from_le_bytes([data[offset], data[offset + 1]]),
            ByteOrder::BigEndian => i16::from_be_bytes([data[offset], data[offset + 1]]),
        };
        values.push(value);
    }
    Ok(values)
}

fn read_int16u_array(data: &[u8], byte_order: ByteOrder, count: usize) -> Result<Vec<u16>> {
    if data.len() < count * 2 {
        return Err(crate::types::ExifError::ParseError(
            "Insufficient data for int16u array".to_string(),
        ));
    }
    let mut values = Vec::with_capacity(count);
    for i in 0..count {
        let offset = i * 2;
        let value = match byte_order {
            ByteOrder::LittleEndian => u16::from_le_bytes([data[offset], data[offset + 1]]),
            ByteOrder::BigEndian => u16::from_be_bytes([data[offset], data[offset + 1]]),
        };
        values.push(value);
    }
    Ok(values)
}

fn read_int16s(data: &[u8], byte_order: ByteOrder) -> Result<i16> {
    if data.len() < 2 {
        return Err(crate::types::ExifError::ParseError(
            "Insufficient data for int16s".to_string(),
        ));
    }
    Ok(match byte_order {
        ByteOrder::LittleEndian => i16::from_le_bytes([data[0], data[1]]),
        ByteOrder::BigEndian => i16::from_be_bytes([data[0], data[1]]),
    })
}

// Subdirectory processing functions
fn process_jpeg2000_main(data: &[u8], byte_order: ByteOrder) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // EXIF at offset Exif

    // Association at offset asoc

    // BinaryDataType at offset bfdb
    if data.len() >= 2 {
        // TODO: Handle format undef
    }

    // BinaryData at offset bidb
    if data.len() >= 2 {
        // TODO: Handle format undef
    }

    // C2PASaltHash at offset c2sh
    if data.len() >= 2 {
        // TODO: Handle format undef
    }

    // CBORData at offset cbor

    // ColorSpecification at offset colr

    // FragmentTable at offset ftbl

    // FileType at offset ftyp

    // GainMapImage at offset hrgm
    if data.len() >= 2 {
        // TODO: Handle format undef
    }

    // ImageHeader at offset ihdr

    // JP2Header at offset jp2h

    // IntellectualProperty at offset jp2i

    // CodestreamHeader at offset jpch

    // CompositingLayerHeader at offset jplh

    // JSONData at offset json

    // JUMBFBox at offset jumb

    // JUMBFDescr at offset jumd

    // JXLCodestream at offset jxlc
    if data.len() >= 2 {
        // TODO: Handle format undef
    }

    // PartialJXLCodestream at offset jxlp
    if data.len() >= 2 {
        // TODO: Handle format undef
    }

    // Label at offset lbl
    if data.len() >= 2 {
        // TODO: Handle format string
    }

    // Resolution at offset res

    // CaptureResolution at offset resc

    // DisplayResolution at offset resd

    // UUIDInfo at offset uinf

    // URL at offset url
    if data.len() >= 2 {
        // TODO: Handle format string
    }

    Ok(tags)
}

fn process_jpeg2000_colorspec(
    data: &[u8],
    byte_order: ByteOrder,
) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // ColorSpecMethod at offset 0

    // ColorSpecPrecedence at offset 1

    // ColorSpecApproximation at offset 2

    Ok(tags)
}

fn process_jpeg2000_filetype(
    data: &[u8],
    byte_order: ByteOrder,
) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // MajorBrand at offset 0
    if data.len() >= 8 {
        // TODO: Handle format undef
    }

    // MinorVersion at offset 1
    if data.len() >= 12 {
        // TODO: Handle format undef
    }

    // CompatibleBrands at offset 2

    Ok(tags)
}

fn process_jpeg2000_imageheader(
    data: &[u8],
    byte_order: ByteOrder,
) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // ImageHeight at offset 0
    if data.len() >= 4 {
        // TODO: Handle format int32u
    }

    // BitsPerComponent at offset 10

    // Compression at offset 11

    // ImageWidth at offset 4
    if data.len() >= 12 {
        // TODO: Handle format int32u
    }

    // NumberOfComponents at offset 8
    if data.len() >= 18 {
        // TODO: Handle format int16u
    }

    Ok(tags)
}

fn process_jpeg2000_jumd(data: &[u8], byte_order: ByteOrder) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // JUMDID at offset id

    // JUMDLabel at offset label

    // JUMDSignature at offset sig

    // JUMDToggles at offset toggles

    // JUMDType at offset type

    Ok(tags)
}

fn process_jpeg2000_captureresolution(
    data: &[u8],
    byte_order: ByteOrder,
) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // CaptureYResolution at offset 0
    if data.len() >= 2 {
        // TODO: Handle format rational32u
    }

    // CaptureXResolution at offset 4
    if data.len() >= 6 {
        // TODO: Handle format rational32u
    }

    // CaptureYResolutionUnit at offset 8

    // CaptureXResolutionUnit at offset 9

    Ok(tags)
}

fn process_jpeg2000_displayresolution(
    data: &[u8],
    byte_order: ByteOrder,
) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // DisplayYResolution at offset 0
    if data.len() >= 2 {
        // TODO: Handle format rational32u
    }

    // DisplayXResolution at offset 4
    if data.len() >= 6 {
        // TODO: Handle format rational32u
    }

    // DisplayYResolutionUnit at offset 8

    // DisplayXResolutionUnit at offset 9

    Ok(tags)
}

pub fn process_tag_0x0_subdirectory(
    data: &[u8],
    byte_order: ByteOrder,
    model: Option<&str>,
) -> Result<Vec<(String, TagValue)>> {
    use crate::expressions::ExpressionEvaluator;
    use crate::processor_registry::ProcessorContext;
    use tracing::debug;
    let count = data.len() / 2;
    debug!(
        "process_tag_0x0_subdirectory called with {} bytes, count={}",
        data.len(),
        count
    );

    // Runtime condition not yet supported: $$valPt =~ /^xml /i
    // Would dispatch to: Image::ExifTool::XMP::Main
    // Runtime condition not yet supported: $$valPt =~ /^exif/i
    // Would dispatch to: Image::ExifTool::Exif::Main
    // Runtime condition not yet supported: $$valPt =~ /^jumb/i
    // Would dispatch to: Image::ExifTool::Jpeg2000::Main
    // Runtime condition not yet supported: $$valPt=~/^JpgTiffExif->JP2(?!Exif\\0\\0)/
    // Would dispatch to: Image::ExifTool::Exif::Main
    // Runtime condition not yet supported: $$valPt=~/^\\x05\\x37\\xcd\\xab\\x9d\\x0c\\x44\\x31\\xa7\\x2a\\xfa\\x56\\x1f\\x2a\\x11\\x3e/
    // Would dispatch to: Image::ExifTool::Exif::Main
    // Runtime condition not yet supported: $$valPt=~/^JpgTiffExif->JP2/
    // Would dispatch to: Image::ExifTool::Exif::Main
    // Runtime condition not yet supported: $$valPt=~/^\\x33\\xc7\\xa4\\xd2\\xb8\\x1d\\x47\\x23\\xa0\\xba\\xf1\\xa3\\xe0\\x97\\xad\\x38/
    // Would dispatch to: Image::ExifTool::IPTC::Main
    // Runtime condition not yet supported: $$valPt=~/^\\x09\\xa1\\x4e\\x97\\xc0\\xb4\\x42\\xe0\\xbe\\xbf\\x36\\xdf\\x6f\\x0c\\xe3\\x6f/
    // Would dispatch to: Image::ExifTool::IPTC::Main
    // Runtime condition not yet supported: $$valPt=~/^\\xbe\\x7a\\xcf\\xcb\\x97\\xa9\\x42\\xe8\\x9c\\x71\\x99\\x94\\x91\\xe3\\xaf\\xac/
    // Would dispatch to: Image::ExifTool::XMP::Main
    // Runtime condition not yet supported: $$valPt=~/^\\xb1\\x4b\\xf8\\xbd\\x08\\x3d\\x4b\\x43\\xa5\\xae\\x8c\\xd7\\xd5\\xa6\\xce\\x03/
    // Would dispatch to: Image::ExifTool::Exif::Main
    // Runtime condition not yet supported: $$valPt=~/^\\x2c\\x4c\\x01\\x00\\x85\\x04\\x40\\xb9\\xa0\\x3e\\x56\\x21\\x48\\xd6\\xdf\\xeb/
    // Would dispatch to: Image::ExifTool::Photoshop::Main
    // Runtime condition not yet supported: $$valPt=~/^c2cs\\x00\\x11\\x00\\x10\\x80\\x00\\x00\\xaa\\x00\\x38\\x9b\\x71/
    // Would dispatch to: Image::ExifTool::CBOR::Main
    // Runtime condition not yet supported: not $$self{IsJXL}
    // Would dispatch to: Image::ExifTool::XMP::XML
    Ok(vec![])
}

pub fn process_tag_0x3_subdirectory(
    data: &[u8],
    byte_order: ByteOrder,
    model: Option<&str>,
) -> Result<Vec<(String, TagValue)>> {
    use crate::expressions::ExpressionEvaluator;
    use crate::processor_registry::ProcessorContext;
    use tracing::debug;
    let count = data.len() / 2;
    debug!(
        "process_tag_0x3_subdirectory called with {} bytes, count={}",
        data.len(),
        count
    );

    // Runtime condition not yet supported: $$self{ColorSpecMethod} == 2 or $$self{ColorSpecMethod} == 3
    // Would dispatch to: Image::ExifTool::ICC_Profile::Main
    Ok(vec![])
}

/// Apply PrintConv for a tag from this module
pub fn apply_print_conv(
    tag_id: u32,
    value: &TagValue,
    _evaluator: &mut ExpressionEvaluator,
    _errors: &mut Vec<String>,
    warnings: &mut Vec<String>,
) -> TagValue {
    if let Some(tag_kit) = JPEG2000_PM_TAG_KITS.get(&tag_id) {
        crate::implementations::generic::apply_fallback_print_conv(
            tag_id,
            &tag_kit.name,
            "Jpeg2000",
            value,
            crate::to_print_conv_ref!(&tag_kit.print_conv),
        )
    } else {
        value.clone()
    }
}

/// Apply ValueConv for a tag from this module
pub fn apply_value_conv(
    tag_id: u32,
    value: &TagValue,
    _errors: &mut Vec<String>,
) -> Result<TagValue> {
    match tag_id {
        0 => {
            if let Some(tag_kit) = JPEG2000_PM_TAG_KITS.get(&tag_id) {
                if let Some(expr) = tag_kit.value_conv {
                    Ok(crate::implementations::missing::missing_value_conv(
                        tag_id,
                        &tag_kit.name,
                        "Jpeg2000",
                        expr,
                        value,
                    ))
                } else {
                    Ok(value.clone())
                }
            } else {
                Ok(value.clone())
            }
        }
        1 => {
            if let Some(tag_kit) = JPEG2000_PM_TAG_KITS.get(&tag_id) {
                if let Some(expr) = tag_kit.value_conv {
                    Ok(crate::implementations::missing::missing_value_conv(
                        tag_id,
                        &tag_kit.name,
                        "Jpeg2000",
                        expr,
                        value,
                    ))
                } else {
                    Ok(value.clone())
                }
            } else {
                Ok(value.clone())
            }
        }
        2 => {
            if let Some(tag_kit) = JPEG2000_PM_TAG_KITS.get(&tag_id) {
                if let Some(expr) = tag_kit.value_conv {
                    Ok(crate::implementations::missing::missing_value_conv(
                        tag_id,
                        &tag_kit.name,
                        "Jpeg2000",
                        expr,
                        value,
                    ))
                } else {
                    Ok(value.clone())
                }
            } else {
                Ok(value.clone())
            }
        }
        _ => {
            // Fall back to missing handler for unknown expressions
            if let Some(tag_kit) = JPEG2000_PM_TAG_KITS.get(&tag_id) {
                if let Some(expr) = tag_kit.value_conv {
                    Ok(crate::implementations::missing::missing_value_conv(
                        tag_id,
                        &tag_kit.name,
                        "Jpeg2000",
                        expr,
                        value,
                    ))
                } else {
                    Ok(value.clone())
                }
            } else {
                Ok(value.clone())
            }
        }
    }
}

/// Check if a tag has subdirectory processing
pub fn has_subdirectory(tag_id: u32) -> bool {
    if let Some(tag_kit) = JPEG2000_PM_TAG_KITS.get(&tag_id) {
        tag_kit.subdirectory.is_some()
    } else {
        false
    }
}

/// Process subdirectory tags and return multiple extracted tags
pub fn process_subdirectory(
    tag_id: u32,
    value: &TagValue,
    byte_order: ByteOrder,
    model: Option<&str>,
) -> Result<HashMap<String, TagValue>> {
    use tracing::debug;
    let mut result = HashMap::new();

    debug!("process_subdirectory called for tag_id: 0x{:04x}", tag_id);

    if let Some(tag_kit) = JPEG2000_PM_TAG_KITS.get(&tag_id) {
        if let Some(SubDirectoryType::Binary { processor }) = &tag_kit.subdirectory {
            debug!("Found subdirectory processor for tag_id: 0x{:04x}", tag_id);
            let bytes = match value {
                TagValue::U16Array(arr) => {
                    debug!("Converting U16Array with {} elements to bytes", arr.len());
                    // Convert U16 array to bytes based on byte order
                    let mut bytes = Vec::with_capacity(arr.len() * 2);
                    for val in arr {
                        match byte_order {
                            ByteOrder::LittleEndian => bytes.extend_from_slice(&val.to_le_bytes()),
                            ByteOrder::BigEndian => bytes.extend_from_slice(&val.to_be_bytes()),
                        }
                    }
                    bytes
                }
                TagValue::U8Array(arr) => arr.clone(),
                _ => return Ok(result), // Not array data
            };

            debug!("Calling processor with {} bytes", bytes.len());
            // Process subdirectory and collect all extracted tags
            match processor(&bytes, byte_order, model) {
                Ok(extracted_tags) => {
                    debug!("Processor returned {} tags", extracted_tags.len());
                    for (name, value) in extracted_tags {
                        result.insert(name, value);
                    }
                }
                Err(e) => {
                    debug!("Processor error: {:?}", e);
                }
            }
        } else {
            debug!(
                "No subdirectory processor found for tag_id: 0x{:04x}",
                tag_id
            );
        }
    } else {
        debug!("Tag not found in TAG_KITS: 0x{:04x}", tag_id);
    }

    Ok(result)
}
