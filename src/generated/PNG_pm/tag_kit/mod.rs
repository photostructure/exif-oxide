//! Modular tag kits with embedded PrintConv for PNG_pm
//!
//! This file is automatically generated by codegen.
//! DO NOT EDIT MANUALLY - changes will be overwritten.
//!
//! Generated from: PNG.pm table: AnimationControl
//!
#![allow(unused_imports)]
#![allow(unused_mut)]
#![allow(dead_code)]
#![allow(unused_variables)]

pub mod datetime;
pub mod interop;
pub mod other;

use crate::expressions::ExpressionEvaluator;
use crate::tiff_types::ByteOrder;
use crate::types::{Result, TagValue};
use std::collections::HashMap;
use std::sync::LazyLock;

#[derive(Debug, Clone)]
pub struct TagKitDef {
    pub id: u32,
    pub name: &'static str,
    pub format: &'static str,
    pub groups: HashMap<&'static str, &'static str>,
    pub writable: bool,
    pub notes: Option<&'static str>,
    pub print_conv: PrintConvType,
    pub value_conv: Option<&'static str>,
    pub subdirectory: Option<SubDirectoryType>,
}

#[derive(Debug, Clone)]
pub enum PrintConvType {
    None,
    Simple(&'static HashMap<String, &'static str>),
    Expression(&'static str),
    Manual(&'static str),
}

/// Type alias for subdirectory processor function
pub type SubDirectoryProcessor =
    fn(&[u8], ByteOrder, Option<&str>) -> Result<Vec<(String, TagValue)>>;

#[derive(Debug, Clone)]
pub enum SubDirectoryType {
    Binary { processor: SubDirectoryProcessor },
}

/// All tag kits for PNG_pm
pub static PNG_PM_TAG_KITS: LazyLock<HashMap<u32, TagKitDef>> = LazyLock::new(|| {
    let mut map: HashMap<u32, TagKitDef> = HashMap::new();

    // datetime tags
    for (id, tag_def) in datetime::get_datetime_tags() {
        // Priority insertion: preserve existing entries with subdirectory processors
        match map.get(&id) {
            Some(existing) if existing.subdirectory.is_some() => {
                // Keep existing tag if it has a subdirectory processor
                if tag_def.subdirectory.is_none() {
                    // Skip this tag - existing one is more important
                    continue;
                }
            }
            _ => {}
        }
        map.insert(id, tag_def);
    }

    // interop tags
    for (id, tag_def) in interop::get_interop_tags() {
        // Priority insertion: preserve existing entries with subdirectory processors
        match map.get(&id) {
            Some(existing) if existing.subdirectory.is_some() => {
                // Keep existing tag if it has a subdirectory processor
                if tag_def.subdirectory.is_none() {
                    // Skip this tag - existing one is more important
                    continue;
                }
            }
            _ => {}
        }
        map.insert(id, tag_def);
    }

    // other tags
    for (id, tag_def) in other::get_other_tags() {
        // Priority insertion: preserve existing entries with subdirectory processors
        match map.get(&id) {
            Some(existing) if existing.subdirectory.is_some() => {
                // Keep existing tag if it has a subdirectory processor
                if tag_def.subdirectory.is_none() {
                    // Skip this tag - existing one is more important
                    continue;
                }
            }
            _ => {}
        }
        map.insert(id, tag_def);
    }

    map
});

// Helper functions for reading binary data
fn model_matches(model: &str, pattern: &str) -> bool {
    // ExifTool regexes are already in a compatible format
    // Just need to ensure proper escaping was preserved

    match regex::Regex::new(pattern) {
        Ok(re) => re.is_match(model),
        Err(e) => {
            tracing::warn!("Failed to compile model regex '{}': {}", pattern, e);
            false
        }
    }
}

fn format_matches(format: &str, pattern: &str) -> bool {
    if let Ok(re) = regex::Regex::new(pattern) {
        re.is_match(format)
    } else {
        tracing::warn!("Failed to compile format regex: {}", pattern);
        false
    }
}

fn read_int16s_array(data: &[u8], byte_order: ByteOrder, count: usize) -> Result<Vec<i16>> {
    if data.len() < count * 2 {
        return Err(crate::types::ExifError::ParseError(
            "Insufficient data for int16s array".to_string(),
        ));
    }
    let mut values = Vec::with_capacity(count);
    for i in 0..count {
        let offset = i * 2;
        let value = match byte_order {
            ByteOrder::LittleEndian => i16::from_le_bytes([data[offset], data[offset + 1]]),
            ByteOrder::BigEndian => i16::from_be_bytes([data[offset], data[offset + 1]]),
        };
        values.push(value);
    }
    Ok(values)
}

fn read_int16u_array(data: &[u8], byte_order: ByteOrder, count: usize) -> Result<Vec<u16>> {
    if data.len() < count * 2 {
        return Err(crate::types::ExifError::ParseError(
            "Insufficient data for int16u array".to_string(),
        ));
    }
    let mut values = Vec::with_capacity(count);
    for i in 0..count {
        let offset = i * 2;
        let value = match byte_order {
            ByteOrder::LittleEndian => u16::from_le_bytes([data[offset], data[offset + 1]]),
            ByteOrder::BigEndian => u16::from_be_bytes([data[offset], data[offset + 1]]),
        };
        values.push(value);
    }
    Ok(values)
}

fn read_int16s(data: &[u8], byte_order: ByteOrder) -> Result<i16> {
    if data.len() < 2 {
        return Err(crate::types::ExifError::ParseError(
            "Insufficient data for int16s".to_string(),
        ));
    }
    Ok(match byte_order {
        ByteOrder::LittleEndian => i16::from_le_bytes([data[0], data[1]]),
        ByteOrder::BigEndian => i16::from_be_bytes([data[0], data[1]]),
    })
}

// Subdirectory processing functions
fn process_png_animationcontrol(
    data: &[u8],
    byte_order: ByteOrder,
) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // AnimationFrames at offset 0

    // AnimationPlays at offset 1

    Ok(tags)
}

fn process_png_primarychromaticities(
    data: &[u8],
    byte_order: ByteOrder,
) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // WhitePointX at offset 0

    // WhitePointY at offset 1

    // RedX at offset 2

    // RedY at offset 3

    // GreenX at offset 4

    // GreenY at offset 5

    // BlueX at offset 6

    // BlueY at offset 7

    Ok(tags)
}

fn process_png_cicodepoints(data: &[u8], byte_order: ByteOrder) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // ColorPrimaries at offset 0

    // TransferCharacteristics at offset 1

    // MatrixCoefficients at offset 2

    Ok(tags)
}

fn process_png_textualdata(data: &[u8], byte_order: ByteOrder) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // TagArtist at offset Artist

    // TagAuthor at offset Author

    // TagCollection at offset Collection

    // TagComment at offset Comment

    // TagCopyright at offset Copyright

    // CreationTime at offset Creation Time

    // TagDescription at offset Description

    // TagDisclaimer at offset Disclaimer

    // TagDocument at offset Document

    // TagLabel at offset Label

    // TagMake at offset Make

    // TagModel at offset Model

    // Photoshop_Profile at offset Raw profile type 8bim

    // EXIF_Profile at offset Raw profile type exif

    // ICC_Profile at offset Raw profile type icc

    // ICC_Profile at offset Raw profile type icm

    // IPTC_Profile at offset Raw profile type iptc

    // XMP_Profile at offset Raw profile type xmp

    // TagSoftware at offset Software

    // TagSource at offset Source

    // TagTimeStamp at offset TimeStamp

    // TagTitle at offset Title

    // PNGWarning at offset Warning

    // XMP at offset XML:com.adobe.xmp

    // AestheticScore at offset aesthetic_score

    // CreateDate at offset create-date

    // ModDate at offset modify-date

    // Tagparameters at offset parameters

    Ok(tags)
}

fn process_png_physicalpixel(
    data: &[u8],
    byte_order: ByteOrder,
) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // PixelsPerUnitX at offset 0
    if data.len() >= 4 {
        // TODO: Handle format int32u
    }

    // PixelsPerUnitY at offset 4
    if data.len() >= 12 {
        // TODO: Handle format int32u
    }

    // PixelUnits at offset 8

    Ok(tags)
}

fn process_png_subjectscale(data: &[u8], byte_order: ByteOrder) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // SubjectUnits at offset 0

    // SubjectPixelWidth at offset 1
    if data.len() >= 4 {
        // TODO: Handle format var_string
    }

    // SubjectPixelHeight at offset 2
    if data.len() >= 6 {
        // TODO: Handle format var_string
    }

    Ok(tags)
}

fn process_png_stereoimage(data: &[u8], byte_order: ByteOrder) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // StereoMode at offset 0

    Ok(tags)
}

fn process_png_virtualpage(data: &[u8], byte_order: ByteOrder) -> Result<Vec<(String, TagValue)>> {
    let mut tags = Vec::new();
    // VirtualPageUnits at offset 2
    if data.len() >= 9 {
        // TODO: Handle format int8u
    }

    Ok(tags)
}

pub fn process_tag_0x0_subdirectory(
    data: &[u8],
    byte_order: ByteOrder,
    model: Option<&str>,
) -> Result<Vec<(String, TagValue)>> {
    use crate::expressions::ExpressionEvaluator;
    use crate::processor_registry::ProcessorContext;
    use tracing::debug;
    let count = data.len() / 2;
    debug!(
        "process_tag_0x0_subdirectory called with {} bytes, count={}",
        data.len(),
        count
    );

    // Runtime condition not yet supported: # set FileType to \"PNG Plus\" if ($$self{VALUE}{FileType} and $$self{VALUE}{FileType} eq \"PNG\") { $$self{VALUE}{FileType} = 'PNG Plus'; } return 1;
    // Would dispatch to: Image::ExifTool::FlashPix::Main
    Ok(vec![])
}

/// Apply PrintConv for a tag from this module
pub fn apply_print_conv(
    tag_id: u32,
    value: &TagValue,
    _evaluator: &mut ExpressionEvaluator,
    _errors: &mut Vec<String>,
    warnings: &mut Vec<String>,
) -> TagValue {
    if let Some(tag_kit) = PNG_PM_TAG_KITS.get(&tag_id) {
        crate::implementations::generic::apply_fallback_print_conv(
            tag_id,
            &tag_kit.name,
            "PNG",
            value,
            crate::to_print_conv_ref!(&tag_kit.print_conv),
        )
    } else {
        value.clone()
    }
}

/// Apply ValueConv for a tag from this module
pub fn apply_value_conv(
    tag_id: u32,
    value: &TagValue,
    _errors: &mut Vec<String>,
) -> Result<TagValue> {
    match tag_id {
        0 => {
            if let Some(tag_kit) = PNG_PM_TAG_KITS.get(&tag_id) {
                if let Some(expr) = tag_kit.value_conv {
                    Ok(crate::implementations::missing::missing_value_conv(
                        tag_id,
                        &tag_kit.name,
                        "PNG",
                        expr,
                        value,
                    ))
                } else {
                    Ok(value.clone())
                }
            } else {
                Ok(value.clone())
            }
        }
        1 => {
            // Compiled arithmetic: $val / 100000
            match value.as_f64() {
                Some(val) => Ok(TagValue::F64(val / 100000.0_f64)),
                None => Ok(value.clone()),
            }
        }
        2 => {
            // Compiled arithmetic: $val / 100000
            match value.as_f64() {
                Some(val) => Ok(TagValue::F64(val / 100000.0_f64)),
                None => Ok(value.clone()),
            }
        }
        3 => {
            // Compiled arithmetic: $val / 100000
            match value.as_f64() {
                Some(val) => Ok(TagValue::F64(val / 100000.0_f64)),
                None => Ok(value.clone()),
            }
        }
        4 => {
            // Compiled arithmetic: $val / 100000
            match value.as_f64() {
                Some(val) => Ok(TagValue::F64(val / 100000.0_f64)),
                None => Ok(value.clone()),
            }
        }
        5 => {
            // Compiled arithmetic: $val / 100000
            match value.as_f64() {
                Some(val) => Ok(TagValue::F64(val / 100000.0_f64)),
                None => Ok(value.clone()),
            }
        }
        6 => {
            // Compiled arithmetic: $val / 100000
            match value.as_f64() {
                Some(val) => Ok(TagValue::F64(val / 100000.0_f64)),
                None => Ok(value.clone()),
            }
        }
        7 => {
            // Compiled arithmetic: $val / 100000
            match value.as_f64() {
                Some(val) => Ok(TagValue::F64(val / 100000.0_f64)),
                None => Ok(value.clone()),
            }
        }
        _ => {
            // Fall back to missing handler for unknown expressions
            if let Some(tag_kit) = PNG_PM_TAG_KITS.get(&tag_id) {
                if let Some(expr) = tag_kit.value_conv {
                    Ok(crate::implementations::missing::missing_value_conv(
                        tag_id,
                        &tag_kit.name,
                        "PNG",
                        expr,
                        value,
                    ))
                } else {
                    Ok(value.clone())
                }
            } else {
                Ok(value.clone())
            }
        }
    }
}

/// Check if a tag has subdirectory processing
pub fn has_subdirectory(tag_id: u32) -> bool {
    if let Some(tag_kit) = PNG_PM_TAG_KITS.get(&tag_id) {
        tag_kit.subdirectory.is_some()
    } else {
        false
    }
}

/// Process subdirectory tags and return multiple extracted tags
pub fn process_subdirectory(
    tag_id: u32,
    value: &TagValue,
    byte_order: ByteOrder,
    model: Option<&str>,
) -> Result<HashMap<String, TagValue>> {
    use tracing::debug;
    let mut result = HashMap::new();

    debug!("process_subdirectory called for tag_id: 0x{:04x}", tag_id);

    if let Some(tag_kit) = PNG_PM_TAG_KITS.get(&tag_id) {
        if let Some(SubDirectoryType::Binary { processor }) = &tag_kit.subdirectory {
            debug!("Found subdirectory processor for tag_id: 0x{:04x}", tag_id);
            let bytes = match value {
                TagValue::U16Array(arr) => {
                    debug!("Converting U16Array with {} elements to bytes", arr.len());
                    // Convert U16 array to bytes based on byte order
                    let mut bytes = Vec::with_capacity(arr.len() * 2);
                    for val in arr {
                        match byte_order {
                            ByteOrder::LittleEndian => bytes.extend_from_slice(&val.to_le_bytes()),
                            ByteOrder::BigEndian => bytes.extend_from_slice(&val.to_be_bytes()),
                        }
                    }
                    bytes
                }
                TagValue::U8Array(arr) => arr.clone(),
                _ => return Ok(result), // Not array data
            };

            debug!("Calling processor with {} bytes", bytes.len());
            // Process subdirectory and collect all extracted tags
            match processor(&bytes, byte_order, model) {
                Ok(extracted_tags) => {
                    debug!("Processor returned {} tags", extracted_tags.len());
                    for (name, value) in extracted_tags {
                        result.insert(name, value);
                    }
                }
                Err(e) => {
                    debug!("Processor error: {:?}", e);
                }
            }
        } else {
            debug!(
                "No subdirectory processor found for tag_id: 0x{:04x}",
                tag_id
            );
        }
    } else {
        debug!("Tag not found in TAG_KITS: 0x{:04x}", tag_id);
    }

    Ok(result)
}
